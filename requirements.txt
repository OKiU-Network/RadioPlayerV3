# RadioPlayerV3 — tgcalls==2.0.0 (required by pytgcalls==2.1.0) wheel layout:
# - Linux manylinux: only cp36–cp39 (no cp310-manylinux on PyPI).
# - Windows/macOS: cp310 wheels exist — use Python 3.10 locally on those OSes.
# - Python 3.11+: no tgcalls 2.0.0 wheels at all.
# Docker/Linux servers: use Python 3.9 (see Dockerfile).
wget
wheel
psutil
heroku3
requests
tgcrypto
ffmpeg-python
python-dotenv
youtube_search
youtube_search_python
pyrogram==2.0.106
tgcalls==2.0.0
pytgcalls==2.1.0
git+https://github.com/asmsafone/downloader
