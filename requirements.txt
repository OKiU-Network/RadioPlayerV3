# RadioPlayerV3 — use Python 3.10.x for installs (pip).
# tgcalls==2.0.0 has PyPI wheels only for cp36–cp310, not Python 3.11+.
# On Python 3.11+, pip only offers tgcalls 3.x dev builds and install fails with pytgcalls==2.1.0.
wget
wheel
psutil
heroku3
requests
tgcrypto
ffmpeg-python
python-dotenv
youtube_search
youtube_search_python
pyrogram==2.0.106
tgcalls==2.0.0
pytgcalls==2.1.0
git+https://github.com/asmsafone/downloader
